data:
  image: buildpack-deps:trusty
  volumes:
    - /designsafe/media:/var/www/designsafe-ci.org/media

django:
  build: .
  env_file: designsafe.env
  volumes:
    - /var/www/designsafe-ci.org/static
  volumes_from:
    - data

nginx:
  image: nginx
  volumes:
    - ./conf/nginx-qa.conf:/etc/nginx/nginx.conf
    - ./certs:/etc/ssl/designsafe-ci.org
    - ./conf/robots.txt:/var/www/designsafe-ci.org/robots.txt
  volumes_from:
    - data
    - django
  links:
    - django:django
  ports:
    - 80:80
    - 443:443