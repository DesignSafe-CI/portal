""" Django app configuration for onboarding app. """

from django.apps import AppConfig


class OnboardingConfig(AppConfig):
    """Django app configuration for onboarding app."""

    name = "designsafe.apps.onboarding"
